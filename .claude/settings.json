{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f pom.xml ] && mvn -q test-compile || true"
          }
        ]
      }
    ]
  }
}
